#include <stdio.h>
#include <string.h>
int main()
{
	int head = 0, tail = 0;
	char ans[505][1000], temp[1000], temp2[1000];
	memcpy(ans[0], "0");
	while(tail <= 500)
	{
		memcpy(ans)
	}
	for(int i = 1;i <= 500;i++)
	{
		for(int j = strlen(ans[i] - 1);j >= 0;j--)
			printf("%c", ans[i][j]);
		printf("\n");
	}
	return 0;
}
